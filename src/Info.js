import React from 'react';

export const Info = (props) => {

    return(
        <div className={"info"}>
            <label id={"rowCount"} title={"Počet záznamů"}>8</label>
        </div>
    );
};